---
applications:
  - name: solution-advisor
    memory: 128M
    buildpacks: 
      - https://github.com/cloudfoundry/nodejs-buildpack
    command: npm start
      docker:
       build:
        context: .
        dockerfile: ./Dockerfile
       environment:
        NODE_ENV: production
        ports:
          - 5000:5000
